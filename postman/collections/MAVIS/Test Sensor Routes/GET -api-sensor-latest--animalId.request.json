{
  "name": "GET /api/sensor/latest/:animalId",
  "request": {
    "method": "GET",
    "url": "{{baseUrl}}/api/sensor/latest/{{animalId}}",
    "description": "Gets the most recent sensor reading for one animal ObjectId."
  }
}
